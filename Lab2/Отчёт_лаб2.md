# Отчет по лабораторной работе №2

## ПОСТАНОВКА ЗАДАЧИ, ЦЕЛЬ РАБОТЫ

Цель работы:  
Научиться реализовывать полный pipeline классификации видео на практике с использованием датасета HowTo100M, предобученной модели VideoMAE для извлечения признаков и алгоритма Logistic Regression для итоговой классификации.

В рамках лабораторной работы решалась задача классификации видеороликов, связанных с health-like тематикой. Исходным источником данных выступал датасет HowTo100M. Так как полный HowTo100M является слишком большим для обработки в Google Colab, была сформирована ограниченная тематическая подвыборка видео.

Постановка задач:

1. Загрузить metadata датасета HowTo100M.
2. Сформировать health-like подвыборку по ключевым словам из metadata.
3. Скачать доступные видеоролики по YouTube ID с помощью `yt-dlp`.
4. Выполнить предобработку видео: извлечь кадры, изменить размер кадров и подготовить их для модели.
5. Использовать предобученную модель VideoMAE как feature extractor.
6. Получить embedding-векторы видеороликов размерности 768.
7. Обучить классификатор Logistic Regression поверх извлечённых признаков.
8. Оценить качество модели с помощью метрик Accuracy, Precision, Recall, F1-score и Top-K accuracy.
9. Провести анализ ошибок: построить confusion matrix, вывести примеры неверных классификаций и оценить ошибки по классам.
10. Сохранить обученную модель и дополнительные артефакты.

## ТЕОРЕТИЧЕСКАЯ БАЗА

### Датасет HowTo100M

HowTo100M — это крупный видеодатасет обучающих YouTube-видео. Он используется в задачах video understanding, video classification и обучения совместных видео-текстовых представлений.

В данной работе полный датасет не использовался, так как он слишком большой для среды Google Colab. Вместо этого была использована metadata HowTo100M, по которой выбирались подходящие видеоролики, а затем доступные видео скачивались по YouTube ID.

Для отбора health-like видео использовались ключевые слова:

- `exercise`
- `workout`
- `yoga`
- `fitness`
- `massage`
- `meditation`
- `stretch`
- `training`
- `therapy`
- `rehab`
- `health`
- `body`
- `muscle`
- `gym`
- `pilates`
- `cardio`

После фильтрации видеоролики были распределены по следующим категориям:

- `fitness`
- `massage`
- `stretch`
- `therapy`

Эти категории использовались как классы для обучения и тестирования модели.

### Предобработка видео

Видео не подавалось в модель напрямую. Сначала из каждого видеоролика извлекались отдельные кадры. Для этого использовалась библиотека OpenCV.

Основные этапы предобработки:

1. Открытие видеофайла с помощью OpenCV.
2. Получение количества кадров в видео.
3. Выбор 16 кадров, равномерно распределённых по ролику.
4. Преобразование цветового формата из BGR в RGB.
5. Изменение размера кадров.
6. Формирование списка кадров для подачи в VideoMAE.

Такой подход позволяет представить видео как последовательность изображений, которую может обработать видеомодель.

### Архитектура VideoMAE

VideoMAE — это модель для обработки видео, основанная на архитектуре Vision Transformer и masked autoencoding. Она анализирует не только отдельные кадры, но и временную структуру видео.

В данной лабораторной работе VideoMAE использовалась как feature extractor. Это означает, что модель не обучалась заново и не дообучалась на выбранной подвыборке. Она применялась для преобразования каждого видео в числовой embedding-вектор.

Каждый видеоролик после обработки VideoMAE представлялся вектором размерности 768. Эти признаки затем использовались для обучения классификатора.

### Классификатор Logistic Regression

Logistic Regression — это алгоритм машинного обучения для классификации. Несмотря на слово “regression” в названии, в данной работе он использовался именно для выбора класса видео.

Входом для Logistic Regression были не пиксели и не кадры напрямую, а embedding-векторы, извлечённые моделью VideoMAE. Поэтому классификатор обучался уже на готовых информативных признаках.

### Top-K Accuracy

Top-K accuracy — это метрика, которая проверяет, находится ли правильный класс среди K наиболее вероятных предсказаний модели.

Обычная Accuracy учитывает только самый вероятный класс. Если правильный класс оказался на втором или третьем месте, обычная Accuracy считает это ошибкой. Top-K accuracy позволяет оценить, насколько часто правильный ответ находится среди нескольких наиболее вероятных вариантов.

В работе использовались:

- `Top-2 Accuracy`
- `Top-3 Accuracy`

Так как всего было 4 класса, Top-4 accuracy не использовалась: при четырёх классах она почти всегда была бы равна 1 и не несла бы полезной информации.

## РЕЗУЛЬТАТЫ РАБОТЫ И ТЕСТИРОВАНИЯ СИСТЕМЫ

В ходе работы был реализован следующий pipeline:

1. Загрузка metadata HowTo100M.
2. Формирование health-like подвыборки.
3. Загрузка доступных YouTube-видео через `yt-dlp`.
4. Извлечение кадров из видеороликов.
5. Получение признаков с помощью VideoMAE.
6. Обучение Logistic Regression.
7. Оценка качества модели.
8. Анализ ошибок модели.

Для тестирования использовалась валидационная выборка из 18 видеороликов. Классификация выполнялась по четырём категориям:

- `fitness`
- `massage`
- `stretch`
- `therapy`

### Общие метрики

Результаты оценки модели:

| Метрика | Значение |
|---|---:|
| Accuracy | 0.5556 |
| Top-2 Accuracy | 0.5556 |
| Top-3 Accuracy | 0.8889 |

Accuracy составила 0.5556. Это означает, что модель правильно классифицировала 10 из 18 видеороликов.

Top-2 Accuracy также составила 0.5556. Это показывает, что правильный класс попадал в два наиболее вероятных варианта с такой же частотой, как и в первое предсказание.

Top-3 Accuracy составила 0.8889. Это означает, что в большинстве случаев правильный класс находился среди трёх наиболее вероятных предсказаний модели.

### Метрики по классам

![metrics](/img/metrics.png)

| Класс | Precision | Recall | F1-score |
|---|---:|---:|---:|
| fitness | 0.7500 | 0.6000 | 0.6667 |
| massage | 0.4286 | 0.7500 | 0.5455 |
| stretch | 0.5000 | 0.5000 | 0.5000 |
| therapy | 0.6667 | 0.4000 | 0.5000 |

Лучший результат по F1-score был получен для класса `fitness`:

```text
F1-score = 0.6667
```

Это говорит о том, что модель лучше всего распознаёт видео, связанные с тренировками и фитнесом.

Класс `massage` имеет высокий Recall = 0.7500, но низкий Precision = 0.4286. Это означает, что модель часто относит видео к классу `massage`, но часть таких предсказаний является ошибочной.

Классы `stretch` и `therapy` имеют F1-score = 0.5000, что говорит о более сложном разделении этих категорий.

### Тест на одном видео

Также был выполнен тест на одном отдельном видеоролике:

```text
Video path: /content/howto100m_project/videos/anoTp_NSsaI.mp4
True label: stretch
Predicted label: stretch
```

Вероятности классов для этого видео:

| Класс | Вероятность |
|---|---:|
| stretch | 0.7234 |
| fitness | 0.2447 |
| therapy | 0.0294 |
| massage | 0.0026 |

Модель корректно определила класс видео как `stretch`.

## АНАЛИЗ ОШИБОК

Для анализа ошибок использовались:

1. confusion matrix;
2. количество верных и неверных предсказаний;
3. примеры неверных классификаций;
4. анализ ошибок по каждому классу.

### Общая статистика ошибок

На валидационной выборке было 18 видеороликов:

```text
Number of validation samples: 18
Number of wrong predictions: 8
Number of correct predictions: 10
```

Таким образом, модель правильно классифицировала 10 видео и ошиблась на 8 видео.

### Примеры неверных классификаций

| № | True label | Predicted label | Наиболее вероятные классы |
|---:|---|---|---|
| 1 | fitness | massage | massage: 0.8507, therapy: 0.1227, fitness: 0.0174 |
| 2 | massage | therapy | therapy: 0.6562, stretch: 0.1713, massage: 0.1060 |
| 3 | therapy | stretch | stretch: 0.5324, massage: 0.3207, therapy: 0.1118 |
| 4 | therapy | fitness | fitness: 0.8518, stretch: 0.1015, massage: 0.0259 |
| 5 | therapy | stretch | stretch: 0.6739, fitness: 0.1550, therapy: 0.0949 |
| 6 | stretch | massage | massage: 0.5872, fitness: 0.2091, therapy: 0.1224 |
| 7 | stretch | massage | massage: 0.5843, therapy: 0.3735, stretch: 0.0306 |
| 8 | fitness | massage | massage: 0.5214, therapy: 0.4399, fitness: 0.0272 |

Из таблицы видно, что модель часто ошибочно относила видео к классу `massage`. Это может быть связано с визуальной похожестью некоторых роликов: например, растяжка, терапевтические упражнения и массаж могут включать похожие сцены с телом человека, медленными движениями и физическим воздействием.

### Ошибки по классам

| Класс | Всего | Верно | Ошибочно | Error rate |
|---|---:|---:|---:|---:|
| fitness | 5 | 3 | 2 | 0.4000 |
| massage | 4 | 3 | 1 | 0.2500 |
| stretch | 4 | 2 | 2 | 0.5000 |
| therapy | 5 | 2 | 3 | 0.6000 |

Наименьшая доля ошибок была у класса `massage`:

```text
error_rate = 0.2500
```

Наибольшая доля ошибок была у класса `therapy`:

```text
error_rate = 0.6000
```

Это означает, что класс `therapy` оказался самым сложным для модели. Вероятная причина заключается в том, что therapy-видео могут быть похожи на fitness, stretch или massage.

### Анализ confusion matrix

![matrix](/img/matrix.png)

Confusion matrix использовалась для визуального анализа того, какие классы модель путает между собой.

Основные направления ошибок:

1. `therapy` → `stretch`
2. `therapy` → `fitness`
3. `stretch` → `massage`
4. `fitness` → `massage`

Эти ошибки объясняются тем, что все выбранные категории связаны с действиями человека, телом, физической активностью или восстановлением. Поэтому визуальные признаки разных классов могут пересекаться.

## АНАЛИЗ РЕЗУЛЬТАТОВ

Полученное значение Accuracy = 0.5556 можно считать приемлемым для небольшой подвыборки HowTo100M. Задача классификации видео является сложной, особенно при малом числе обучающих примеров и автоматической разметке по metadata.



Особенно важным результатом является Top-3 Accuracy = 0.8889. Это означает, что в 88.89% случаев правильный класс находился среди трёх наиболее вероятных предсказаний. Таким образом, даже когда модель ошибалась в первом варианте, она часто всё равно относила правильный класс к числу наиболее вероятных.

Top-2 Accuracy оказалась равна обычной Accuracy и составила 0.5556. Это означает, что в ошибочных случаях правильный класс чаще всего находился не на втором, а ниже — обычно на третьем месте.

Лучшие результаты модель показала для класса `fitness`. Это может быть связано с тем, что фитнес-видео имеют более ярко выраженные визуальные признаки: упражнения, тренировки, активные движения и спортивную обстановку.

Наиболее сложным классом оказался `therapy`. Для него error_rate составил 0.6000. Это связано с тем, что терапевтические видео могут быть визуально похожи на растяжку, лёгкую тренировку или массаж.

## ВЫВОДЫ ПО РАБОТЕ

В ходе выполнения лабораторной работы были решены все поставленные задачи:

1. Была загружена metadata датасета HowTo100M.
2. Была сформирована тематическая health-like подвыборка видеороликов.
3. Были скачаны доступные видео по YouTube ID с помощью `yt-dlp`.
4. Из каждого видео были извлечены кадры.
5. Предобученная модель VideoMAE была использована для получения embedding-векторов.
6. Поверх признаков VideoMAE был обучен классификатор Logistic Regression.
7. Качество модели было оценено с помощью Accuracy, Precision, Recall, F1-score и Top-K accuracy.
8. Был выполнен анализ ошибок с использованием confusion matrix и примеров неверных классификаций.

Основные выводы:

1. Использование предобученной VideoMAE позволяет решать задачу классификации видео даже при ограниченных вычислительных ресурсах.
2. Logistic Regression показала работоспособность при классификации embedding-векторов, полученных из видео.
3. Модель достигла Accuracy = 0.5556 на валидационной выборке.
4. Top-3 Accuracy = 0.8889 показывает, что правильный класс в большинстве случаев находится среди трёх наиболее вероятных вариантов.
5. Лучшие результаты получены для класса `fitness`, где F1-score составил 0.6667.
6. Наиболее сложным классом оказался `therapy`, для которого error_rate составил 0.6000.
7. Основные ошибки связаны с визуальной похожестью категорий `fitness`, `stretch`, `therapy` и `massage`.
8. Для повышения качества необходимо увеличить размер датасета и улучшить разметку категорий.

Цель работы достигнута: был реализован полный pipeline классификации видео на основе HowTo100M, VideoMAE и Logistic Regression. Удовльствие получено!


## ИСПОЛЬЗОВАННЫЕ ИСТОЧНИКИ

1. https://huggingface.co/docs/transformers/model_doc/videomae
2. https://huggingface.co/datasets/HuggingFaceM4/howto100m
3. https://github.com/yt-dlp/yt-dlp
4. https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LogisticRegression.html
5. https://scikit-learn.org/stable/modules/generated/sklearn.metrics.top_k_accuracy_score.html
6. https://docs.opencv.org/4.x/
7. https://pytorch.org/
